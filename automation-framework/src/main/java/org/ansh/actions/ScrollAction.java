package org.ansh.actions;

import org.openqa.selenium.WebDriver;

public class ScrollAction {

    private WebDriver driver;
    public ScrollAction(WebDriver driver) {
        this.driver = driver;
    }
}
